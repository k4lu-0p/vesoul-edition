{% extends "vesoul-edition/base.html.twig" %}


{% block content %}

    <div id="compte-page" class="pt-4 pb-4 d-flex">

        <nav id="menu-compte" class="col-3 align-items-start">
            <h2 class="text-center">Mon compte</h2>
            <ul>
                <a class="list active" href="{{ path('dashboard_user_informations' )}}">
                    <li>INFORMATIONS</li>
                </a>
                <a class="list" href="{{ path('dashboard_user_addresses')}}">
                    <li>ADRESSES</li>
                </a>
                <a class="list" href="{{ path('dashboard_user_commands')}}">
                    <li>COMMANDES</li>
                </a>
            </ul>
        </nav>

        <div id="infos-compte" class="col-9 d-flex justify-content-center">

            <div class="wrapper-infos d-flex flex-column ml-3 justify-content-center">

                <div class="title-wrapper">
                    <div class="title-compte">
                        <h2>Mes informations</h2>
                    </div>
                </div>

                <div class="infos-content">

                {# {{ form_start(form, {'attr' : {'class': 'form-signin d-flex flex-column justify-content-center align-items-center' }}) }}

                    {{ form_row(form.username, {'label' : false ,'attr' : {'class': 'form-control mb-4', 'placeholder' : 'Adresse mail', 'id' : 'inputUser'}}) }}

                    {{ form_row(form.tel, {'label' : false ,'attr' : {'class': 'form-control mb-4', 'placeholder' : 'Téléphone', 'id' : 'inputPassword'}}) }}

                    {{ form_row(form.password, {'label' : false ,'attr' : {'class': 'form-control mb-3', 'placeholder' : 'Mot de passe', 'id' : 'inputPassword'}}) }}

                    {{ form_row(form.password, {'label' : false ,'attr' : {'class': 'form-control mb-3', 'placeholder' : 'Nouveau mot de passe', 'id' : 'inputNew'}}) }}
                    
                    {{ form_row(form.password, {'label' : false ,'attr' : {'class': 'form-control mb-3', 'placeholder' : 'Confirmation du nouveau mot de passe', 'id' : 'inputNewConfirm'}}) }}
                    
                    <button class="btn-inscription btn btn-lg mt-2 px-3" type="submit">Enregistrer les modifications</button>

                {{ form_end(form) }} #}

                    <form action="" method="POST">
                        <div class="d-flex flex-column">
                            <div class="d-flex justify-content-between">
                                <input class="col-5" type="email" placeholder="Adresse mail">
                                <input class="col-5" type="text" placeholder="Téléphone">
                            </div>
        
                            <input class="changing-pass" type="password" placeholder="Mot de passe">
                            <input class="changing-pass" type="password" placeholder="Nouveau mot de passe">
                            <input class="changing-pass" type="password" placeholder="Confirmer le nouveau mot de passe">
                        </div>
                        <div class="d-flex justify-content-center align-items-end">
                            <button class="btn">Enregistrer les modifications</button>
                        </div>
                    </form>

                </div>

            </div>


        </div>

    </div>


{% endblock %}