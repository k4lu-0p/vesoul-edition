{% extends "front/baseFront.html.twig" %}


{% block content %}

    <div id="compte-page" class="pt-4 pb-4 d-flex">

        <nav id="menu-compte" class="col-3">
            <ul>
                <a class="list" href="{{ path('compte-informations' )}}">
                    <li>INFORMATIONS</li>
                </a>
                <a class="list active" href="{{ path('compte-adresses' )}}">
                    <li>ADRESSES</li>
                </a>
                <a class="list" href="{{ path('compte-commandes' )}}">
                    <li>COMMANDES</li>
                </a>
            </ul>
            <div class="space"></div>
        </nav>
        
        <div id="adresses-compte" class="col-9 d-flex justify-content-center">

            <div class="wrapper-adresses d-flex flex-column">

            {% for adresse in adresses %}

                <div class="list-adresse">
                    <p>{{ adresse.number }} {{ adresse.type }} {{ adresse.street }} {{ adresse.city }} {{ adresse.cp }} {{ adresse.country }} {{ adresse.additional }}</p>
                </div>

            {% endfor %}

            </div>


        </div>

    </div>


{% endblock %}